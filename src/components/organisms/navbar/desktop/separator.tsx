/**
 * A separator element between sections in the navigation bar.
 * @returns A horizontal line element
 */
export function DesktopNavbarSeparator() {
  return <hr className='h-8 w-0.5 bg-neutral-400/20 dark:bg-neutral-600/20' />;
}
